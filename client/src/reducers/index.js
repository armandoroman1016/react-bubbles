const initialState = {
    colors: [],
    error: '',
    isLoggingIn : false,
    hasTokenFalse: false,
}

export const reducer = (state = initialState, action ) => {
    switch(action.type){
        default :
            return state 
    }
}
